from django.apps import AppConfig

# This file includes application configuration for
# the "Exams" app by configuring its attributes.


class ExamsConfig(AppConfig):
	default_auto_field = 'django.db.models.BigAutoField'
	name = 'Exams'

# 3